<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fsd.data.mapper.UserInfoMapper">
    <select id="selectUserByName" resultType="com.fsd.data.entity.UserInfoDB">
	    select id, user_name, password, user_type, email, mobile_num, confirmed, update_ts, veri_code  from user_info where user_name = #{userName}
	</select>
</mapper>